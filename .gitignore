.last-failed.json

# Generated LuaLS IDE artifacts — prova writes these on any run, and `prova ide setup` rewrites
# them. `.luarc.json` holds absolute paths into THIS machine's prova data dir and plugin cache, so
# it is never shareable; `annotations/` is regenerated from the installed prova and its plugins.
/.luarc.json
/annotations/
